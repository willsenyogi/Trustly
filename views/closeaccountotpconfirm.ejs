<div
  style="
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    flex-direction: row;
    width: 100vw;
    flex-wrap: wrap;
  "
>
  <img
    src="/assets/trustly-logo.png"
    style="border-right: 2px solid white; margin: 30px"
  />
  <form action="/api/profile/closeaccount/otpconfirm/true/close" method="POST">
    <p>Enter password :</p>
    <input name="password" style="width: 300px; height: 40px; border-radius: 20px;" type="password" required /></a>
    <p>Enter access code :</p>
    <input id="accesscode" name="accesscode" style="width: 300px; height: 40px; border-radius: 20px;" type="password" required /></a>
   <% if (closeMessage2 && closeMessage2.length > 0) { %>
    <div style="color: red">
      <ul>
        <% closeMessage2.forEach(function(msg) { %>
        <li><%= msg %></li>
        <% }) %>
      </ul>
    </div>
    <% } %>
    <br>
    <button type = "submit" style="width: 100px; height: 40px; border-radius: 20px; color: black; background-color: whitesmoke; border: none;margin-top: 10px;">
      Submit
    </button>
  </form>
</div>

<script>
  document.getElementById('accesscode').addEventListener('input', function (e) {
      this.value = this.value.replace(/[^0-9]/g, '');
  });
</script>